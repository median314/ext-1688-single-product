//  import translate from 'translate-google';

// export const translateText = async (text) => {
//     try {
//       const translation = await translate(text, { from: 'zh-cn', to: 'en' });
//       return translation; // Get translated text
//     } catch (error) {
//       console.error('Error translating text:', error);
//       return null;
//     }
//   };